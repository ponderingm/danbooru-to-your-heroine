# TODO: ローカル開発環境（Devサーバー）と本番分離の整備

## 概要
Coolifyの本番環境とローカル開発環境で静的資産（`src/web`）のバインドマウント競合を解消し、ローカルでビルド待ち時間ゼロでUI開発（即時プレビュー）を行える環境を整備する。

## タスク一覧
1. [ ] `src/config.py` でポート（`PORT` / `API_PORT`）とホスト（`HOST` / `API_HOST`）の環境変数オーバーライドに対応。
2. [ ] ローカル開発用サーバー起動スクリプト `scripts/dev_server.sh` を作成（実行権限付与、ポート競合回避デフォルト `8898`）。
3. [ ] 動作確認（`scripts/dev_server.sh` でサーバーが起動し、即座にUIが配信されることをテスト）。
4. [ ] Coolify 上の本番コンテナの `src/web` バインドマウント設定（Storage ID 63）解除の手順または実行。
5. [ ] 完了ドキュメント `doc/done_local_dev_setup.md` の作成。
